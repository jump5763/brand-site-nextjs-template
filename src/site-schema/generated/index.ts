// Generated by schema:generate. Do not edit.
import type { ReactNode } from 'react';
import type { SiteSection } from './types';
import definition0 from "../../sections/cta.story-links/definition";
import definition1 from "../../sections/faq.default/definition";
import definition2 from "../../sections/features.device-showcase/definition";
import definition3 from "../../sections/features.icon-grid/definition";
import definition4 from "../../sections/features.media-grid/definition";
import definition5 from "../../sections/features.story-disclosures/definition";
import definition6 from "../../sections/hero.default/definition";
import definition7 from "../../sections/hero.intro/definition";
import definition8 from "../../sections/locations.featured-single/definition";
import definition9 from "../../sections/locations.map-list/definition";
import definition10 from "../../sections/menu.catalog/definition";
import definition11 from "../../sections/products.carousel/definition";
import definition12 from "../../sections/reviews.carousel/definition";
import definition13 from "../../sections/story.sequence/definition";
import definition14 from "../../sections/story.split/definition";

export type SectionId = "cta.story-links" | "faq.default" | "features.device-showcase" | "features.icon-grid" | "features.media-grid" | "features.story-disclosures" | "hero.default" | "hero.intro" | "locations.featured-single" | "locations.map-list" | "menu.catalog" | "products.carousel" | "reviews.carousel" | "story.sequence" | "story.split";
export const sectionRegistry = {
  "cta.story-links": definition0,
  "faq.default": definition1,
  "features.device-showcase": definition2,
  "features.icon-grid": definition3,
  "features.media-grid": definition4,
  "features.story-disclosures": definition5,
  "hero.default": definition6,
  "hero.intro": definition7,
  "locations.featured-single": definition8,
  "locations.map-list": definition9,
  "menu.catalog": definition10,
  "products.carousel": definition11,
  "reviews.carousel": definition12,
  "story.sequence": definition13,
  "story.split": definition14,
} as const satisfies { [Key in SectionId]: { readonly id: Key } };

export function renderSection(section: SiteSection): ReactNode {
  const identity = section.type + '.' + section.variant;
  if (section.type === "cta" && section.variant === "story-links") return definition0.render(section);
  if (section.type === "faq" && section.variant === "default") return definition1.render(section);
  if (section.type === "features" && section.variant === "device-showcase") return definition2.render(section);
  if (section.type === "features" && section.variant === "icon-grid") return definition3.render(section);
  if (section.type === "features" && section.variant === "media-grid") return definition4.render(section);
  if (section.type === "features" && section.variant === "story-disclosures") return definition5.render(section);
  if (section.type === "hero" && section.variant === "default") return definition6.render(section);
  if (section.type === "hero" && section.variant === "intro") return definition7.render(section);
  if (section.type === "locations" && section.variant === "featured-single") return definition8.render(section);
  if (section.type === "locations" && section.variant === "map-list") return definition9.render(section);
  if (section.type === "menu" && section.variant === "catalog") return definition10.render(section);
  if (section.type === "products" && section.variant === "carousel") return definition11.render(section);
  if (section.type === "reviews" && section.variant === "carousel") return definition12.render(section);
  if (section.type === "story" && section.variant === "sequence") return definition13.render(section);
  if (section.type === "story" && section.variant === "split") return definition14.render(section);
  throw new Error('SECTION_UNKNOWN: ' + identity);
}
