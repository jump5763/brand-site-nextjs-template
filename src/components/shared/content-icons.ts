import { Store, Leaf, Clock, Sprout, Bike } from "lucide-react";

// Shared icon vocabulary for benefit, value, and service lists.
export const contentIcons = {
  store: Store,
  leaf: Leaf,
  clock: Clock,
  sprout: Sprout,
  bike: Bike,
};
