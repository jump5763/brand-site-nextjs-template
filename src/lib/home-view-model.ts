/**
 * Transitional home view model. M3 will replace these defaults with the
 * validated Site Schema loader while keeping the component prop boundary.
 */
export {
  featuredIds,
  locations,
  money,
  productById,
  products,
  reviews,
} from "@/lib/site";

export type { Review } from "@/lib/site";
